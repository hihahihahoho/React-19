import * as React from "react"
import { WeeksProps } from "react-day-picker"

const CalendarWeeks: React.FC<WeeksProps> = ({ ...props }) => {
  return <div {...props} />
}

export default CalendarWeeks
