# Missing components

Install these from native shadcn/ui, not the registry:

Accordion, Aspect Ratio, Breadcrumb, Card, Collapsible, Command (basic), Context Menu, Dropdown Menu, Hover Card, Label, Menubar, Navigation Menu, Popover (basic), Progress (linear), Resizable, Separator, Sheet, Skeleton, Toggle, Toggle Group, Tooltip.

From Origin UI instead:
Stepper, Timeline.
